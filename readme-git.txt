git clone https://github.com/marcobuc/my-first-repo.git

cd my-first-repo

git config user.email "you@example.com"
git config user.name "Your Name"

git status
git pull origin master

gitg
  stage
  commit

git push origin master 

